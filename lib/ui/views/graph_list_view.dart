import 'package:flutter/material.dart';

class GraphListView extends StatelessWidget {
  const GraphListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
